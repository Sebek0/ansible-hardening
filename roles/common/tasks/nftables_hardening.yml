---
- name: Deploy nftables config
  ansible.builtin.template:
    src: nftables_hardening.j2
    dest: /etc/nftables.conf
    owner: root
    group: root
    mode: "0640"
    validate: "/usr/sbin/nft -c -f %s"
  notify: restart nftables
